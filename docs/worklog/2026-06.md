# Worklog — 2026-06

## 2026-06-28 — Stage 3 Mercado Pago for event budgets

### User goal

Continue Stage 3 and reuse the Mercado Pago integration from `/Users/braulio/La-Abeja` for Caja Moments.

### Starting state

- **Branch:** `eventos`
- **Base commit:** `f635541`
- **Pre-existing working-tree changes:** uncommitted Stage 1-2 event module changes in `backend/cashflow/*`, `frontend/src/App.jsx`, migrations `0002` and `0003`, plus new `AGENTS.md`/`docs/`.
- **Known pre-existing failures:** none documented.

### Scope inspected

- `AGENTS.md`, `docs/PROJECT_STATE.md`, `docs/adr/README.md`, current git status/diff.
- Caja Moments event/budget models, serializers, services, views, URLs, tests, settings, frontend event screen.
- La Abeja payment reference files: `backend/apps/payments/models.py`, `mercadopago.py`, `serializers.py`, `views.py`, `services.py`, `webhook_utils.py`, `urls.py`, and settings/requirements entries.

### Plan

1. Add payment attempt and webhook-log persistence -> verify with migration drift check.
2. Add preference creation and webhook sync endpoints -> verify with focused backend tests using mocked Mercado Pago.
3. Add frontend checkout action -> verify with Vite build.

### Implementation

- Added `EventBudgetPayment` and `EventBudgetPaymentWebhookLog`.
- Added Mercado Pago settings and `mercadopago==3.1.1`.
- Added preference creation, signature validation, webhook deduplication, approved-payment integrity checks, and budget approval sync.
- Added API routes:
  - `POST /api/event-budget-payments/create-preference/`
  - `POST /api/event-budget-payments/webhook/`
- Added a simple frontend payment panel that prepares a preference and opens the returned checkout URL.
- Added admin registration and Stage 3 tests.

### Plan deviations

- Kept frontend as redirect checkout instead of adding Mercado Pago React SDK; this satisfies Stage 3 with less dependency surface.

### Verification evidence

- `.venv/bin/python backend/manage.py makemigrations --check --dry-run`
  - **Stage:** after change
  - **Scope:** model/migration consistency
  - **Result:** passed, exit 0
- `.venv/bin/python backend/manage.py test cashflow`
  - **Stage:** after change
  - **Scope:** focused backend regression plus Stage 3 payment tests
  - **Result:** passed, exit 0, 21 tests
- `npm run build` from `frontend/`
  - **Stage:** after change
  - **Scope:** frontend compile/build
  - **Result:** passed, exit 0
- `git diff --check`
  - **Stage:** after change
  - **Scope:** whitespace errors
  - **Result:** passed, exit 0
- **Skipped checks:** full backend suite not run separately because the focused `cashflow` suite is the affected backend app.

### Decisions

- [ADR-0002](../adr/0002-mercadopago-for-event-budget-payments.md) — Use Mercado Pago for event budget payments — source: `USER_INSTRUCTION`, `REPOSITORY_EVIDENCE`.

### Assumptions

- **Confirmed:** the user allowed copying the integration shape from La Abeja.
- **Confirmed:** Stage 3 payable object is the event budget.
- **Unconfirmed:** production Mercado Pago credentials, webhook secret, collector ID, and public URLs.

### Rejected approaches and notable failures

- **Approach:** frontend Mercado Pago React SDK.
  - **Result/root cause:** unnecessary for redirect checkout.
  - **Why it should not be retried unchanged:** add it only if embedded Wallet UX is explicitly needed.
- **Command:** first `cashflow` test run.
  - **Result/root cause:** failed because the test patched `create_budget_preference` but `MercadoPagoClient.__init__` correctly rejected missing credentials.
  - **Why it should not be retried unchanged:** mock the client object or set credentials when testing preference creation without network.
- **Command:** `.venv/bin/pip install -r backend/requirements.txt` with `mercadopago==3.2.0`.
  - **Result/root cause:** failed because pip offered versions only through `3.1.1` for this Python 3.9 environment.
  - **Why it should not be retried unchanged:** keep `3.1.1` unless the project upgrades Python or verifies `3.2.0` availability.

### Remaining risks or TODOs

- Configure real production Mercado Pago env vars before live checkout.
- Decide later whether approved online payments should auto-create a `CashMovement` and which account receives the money.

### Handoff state

- **Final working tree:** uncommitted Stage 1-3 event-module changes plus docs; no files reverted.
- **Current behavior:** authenticated users can prepare a Mercado Pago preference for a nonzero event budget; webhooks can approve matching budget payments.
- **Next recommended action:** configure sandbox env vars and run one sandbox checkout/webhook end to end.

## 2026-06-28 — Stage 4 automatic cash reconciliation

### User goal

Do the remaining Stage 4 work after Mercado Pago checkout: close the approved-payment loop into caja and show cobranzas.

### Starting state

- **Branch:** `eventos`
- **Base commit:** `f635541`
- **Pre-existing working-tree changes:** uncommitted Stage 1-3 event-module changes from the previous entry.
- **Known pre-existing failures:** none from focused checks.

### Scope inspected

- Existing Stage 3 payment model/service/webhook code.
- Seeded `MERCADO PAGO` account and `COBRO_EVENTO` movement code.
- Frontend reference loading and event screen payment panel.

### Plan

1. Link approved payments to one cash movement -> verify idempotent sync test.
2. Expose payment attempts read-only -> verify API tests and frontend build.
3. Document the cash reconciliation policy -> verify docs and diff check.

### Implementation

- `EventBudgetPayment.cash_movement`: one-to-one link to the generated income movement.
- `sync_event_budget_payment`: approved payments now create one confirmed `CashMovement` with account `MERCADOPAGO_ACCOUNT_NAME` or `MERCADO PAGO`, code `COBRO_EVENTO`, event link, and payment method `Mercado Pago`.
- `EventBudgetPaymentViewSet`: read-only list/detail endpoint at `/api/event-budget-payments/`.
- `frontend/src/App.jsx`: loads event-budget payments and shows a small cobranzas table for the selected event.
- Added migration `0005_eventbudgetpayment_cash_movement.py`.

### Plan deviations

- None.

### Verification evidence

- `.venv/bin/python backend/manage.py makemigrations --check --dry-run`
  - **Stage:** after change
  - **Scope:** model/migration consistency
  - **Result:** passed, exit 0
- `.venv/bin/python backend/manage.py test cashflow`
  - **Stage:** after change
  - **Scope:** focused backend regression and Stage 4 idempotent movement creation
  - **Result:** passed, exit 0, 21 tests
- `npm run build` from `frontend/`
  - **Stage:** after change
  - **Scope:** frontend compile/build
  - **Result:** passed, exit 0

### Decisions

- [ADR-0003](../adr/0003-auto-record-approved-budget-payments.md) — Auto-record approved budget payments as cash movements — source: `USER_INSTRUCTION`, `REPOSITORY_EVIDENCE`.

### Assumptions

- **Confirmed:** the user asked to do all remaining Stage 4 work.
- **Confirmed:** existing seed data has the default Mercado Pago account and event income code.
- **Unconfirmed:** production may override the destination account name with `MERCADOPAGO_ACCOUNT_NAME`.

### Rejected approaches and notable failures

- **Approach:** per-payment account picker.
  - **Result/root cause:** unnecessary until there is a real multi-account settlement need.
  - **Why it should not be retried unchanged:** default account setting covers current operations with less UI.

### Remaining risks or TODOs

- Real sandbox/live checkout still depends on production Mercado Pago credentials and public webhook URL.
- If the Mercado Pago account/day is already closed, the movement creation will raise validation rather than mutate a closed caja day.

### Handoff state

- **Final working tree:** uncommitted Stage 1-4 event-module changes plus docs; no files reverted.
- **Current behavior:** approved Mercado Pago budget payments approve the budget and create exactly one linked income cash movement.
- **Next recommended action:** run a sandbox payment with real credentials and public webhook URL.

## 2026-06-29 — System relevamiento and gap audit

### User goal

Produce a complete system survey and identify what is missing.

### Starting state

- **Branch:** `eventos`
- **Base commit:** `eventos`
- **Pre-existing working-tree changes:** uncommitted event-module, Mercado Pago, docs, and migration files.

### Scope inspected

- Repository instructions, project state, ADR index, latest worklog entries, git status/diff.
- Backend models, services, serializers, views, filters, URLs, settings, seed command, migrations, tests, deploy scripts.
- Frontend `App.jsx`, Vite package/scripts, env examples, README, Render blueprint.

### Plan

1. Map documented decisions and current architecture -> verify by reading `AGENTS.md`, `docs/PROJECT_STATE.md`, ADRs, worklog, README.
2. Audit backend/frontend/integrations/deploy -> verify by targeted source reads and symbol searches.
3. Run proportional checks -> verify with backend tests, migration checks, frontend build, deploy check.

### Verification evidence

- `.venv/bin/python backend/manage.py makemigrations --check --dry-run` from repo root: passed, exit 0.
- `.venv/bin/python backend/manage.py test cashflow` from repo root: passed, exit 0, 21 tests.
- `../.venv/bin/python manage.py test` from `backend/`: passed, exit 0, 21 tests.
- `npm run build` from `frontend/`: passed, exit 0.
- `git diff --check`: passed, exit 0.
- `env DEBUG=False SECRET_KEY=prod-like-secret-key-with-enough-length-for-django-checks ALLOWED_HOSTS=caja-moments-api.onrender.com CORS_ALLOWED_ORIGINS=https://caja-moments-web.onrender.com CSRF_TRUSTED_ORIGINS=https://caja-moments-web.onrender.com SECURE_SSL_REDIRECT=True .venv/bin/python backend/manage.py check --deploy`: passed, exit 0.
- `.venv/bin/python backend/manage.py showmigrations cashflow`: local SQLite has `0004` and `0005` unapplied.

### Findings summary

- Core app is coherent and checks pass, but the event/payment work is still uncommitted.
- Local DB needs pending migrations before exercising payment/cobranzas locally.
- Mercado Pago production/sandbox credentials and real webhook flow are still unverified.
- Frontend reference loading only consumes the first DRF page; operational screens can miss rows after 50 records.
- Authorization is currently coarse: any authenticated user can mutate most resources.
- Deploy config lacks the documented full frontend/static-site blueprint and contains a default admin password value.
- Refunds/chargebacks update payment status but do not yet reverse/void the generated cash movement.

### Decisions

- No ADR created; this was an audit, not a new durable architecture decision.

### Handoff state

- **Final working tree:** original uncommitted work plus this worklog entry; no code changed.
- **Next recommended action:** commit/stabilize current event module, apply local migrations, then fix the first-page frontend data limit before adding new features.
