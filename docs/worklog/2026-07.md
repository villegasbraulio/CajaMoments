# Worklog — 2026-07

## 2026-07-06 — README use cases and data model

### User goal

Update or create the README with all use cases and the data model.

### Starting state

- **Branch:** `eventos`
- **Base commit:** `2a4e393 update mp`
- **Pre-existing working-tree changes:** none from `git status --short --branch`

### Scope inspected

- `AGENTS.md`, `docs/PROJECT_STATE.md`, `docs/adr/README.md`, latest `docs/worklog/2026-06.md`
- `README.md`
- Backend models, serializers, filters, views, URLs, services, requirements
- Frontend navigation and screens in `frontend/src/App.jsx`
- Render blueprint and package metadata

### Plan

1. Map documented and implemented behavior -> verify by targeted reads of models, views, URLs, frontend screens, and project memory.
2. Rewrite README around current behavior -> verify by checking key sections and Markdown content.
3. Update project memory/worklog -> verify with `git diff --check` and final git status.

### Implementation

- Replaced the old phase-oriented README with a current operational README.
- Added full use-case coverage for access, dashboard, caja, accounts, transfers, closes, providers, staff, clients/events, budgets, Mercado Pago, taxes/reminders, reports, API, commands, env vars, seeds, and deploy.
- Added a compact data-model map with entities, fields, relationships, and business rules.
- Corrected `docs/PROJECT_STATE.md` so it no longer claims event/payment work is uncommitted.

### Decisions

- No ADR created. This was documentation of existing repository behavior, not a new durable technical decision.

### Assumptions

- **Confirmed by repository evidence:** use cases and model fields were derived from code, tests, docs, and configuration.
- **Unconfirmed:** none that affect behavior; Mercado Pago sandbox/live validation remains an existing known risk.

### Verification evidence

- `rg -n "## Casos de uso|## Modelo de datos|## API|SEÑA_EVENTO|Mercado Pago|EventBudgetPayment|/api/event-budget-payments/create-preference/" README.md`
  - **Stage:** after documentation update
  - **Scope:** README key sections, Mercado Pago coverage, model coverage, endpoint coverage, exact seeded event-payment code
  - **Result:** passed, exit 0
- `git diff --check -- README.md docs/PROJECT_STATE.md`
  - **Stage:** after documentation update
  - **Scope:** tracked documentation whitespace check
  - **Result:** passed, exit 0
- `awk '/[ \t]$/ { print FILENAME ":" FNR ": trailing whitespace"; bad=1 } END { exit bad }' README.md docs/PROJECT_STATE.md docs/worklog/2026-07.md`
  - **Stage:** after documentation update
  - **Scope:** trailing-whitespace check including new worklog file
  - **Result:** passed, exit 0
- `git status --short --branch`
  - **Stage:** after documentation update
  - **Scope:** final working tree
  - **Result:** exit 0; modified `README.md`, modified `docs/PROJECT_STATE.md`, untracked `docs/worklog/2026-07.md`

### Remaining risks or TODOs

- Run real Mercado Pago sandbox/live checkout and refund validation before production payment use.

## 2026-07-07 — Event operations and graduation ticket sales

### User goal

Extend event operations with complete editing/detail, optional item payments, movement receipts, searchable selects, and add public graduation ticket checkout via Mercado Pago with email summary.

### Starting state

- **Branch:** `eventos`
- **Base commit:** `2a4e393 update mp`
- **Pre-existing working-tree changes:** modified `README.md`, modified `docs/PROJECT_STATE.md`, untracked `docs/worklog/2026-07.md`
- **Baseline checks before editing:** focused backend tests pass, full backend tests pass, migration drift check pass, frontend build pass.

### Scope inspected

- `AGENTS.md`, `README.md`, `docs/PROJECT_STATE.md`, ADR-0002 through ADR-0005, current `docs/worklog/2026-07.md`
- Backend models, serializers, services, views, URLs, admin, tests, settings, filters
- Frontend single-page app in `frontend/src/App.jsx`

### Plan

1. Extend backend models/services/API/tests/migrations -> verify with `manage.py test cashflow`.
2. Extend frontend views and reusable searchable selects -> verify with `npm run build`.
3. Update README, project state, worklog, and ADRs -> verify with canonical checks and final diff/status.

### Implementation

- Added `Event.schedule_notes`, `Event.sketch`, and `Event.function_notes`; event cost remains `EventBudget.total()`.
- Added `EventBudgetPayment.budget_item`, item manual payment endpoint, item Mercado Pago preference support, and event overview detail arrays.
- Added minimal PDF receipt/comprobante generation for confirmed `CashMovement` plus `/api/cash-movements/{id}/receipt/`.
- Added `GraduationEvent`, `Graduate`, `TicketPurchase`, and ticket webhook log models, APIs, public endpoints, email summary, Mercado Pago preference/webhook sync, cash movement creation, and refund reversal.
- Added frontend searchable selects, movement detail/edit/PDF download, provider and employee editing, event detail tables, item payment actions, graduation admin screen, and `/egresados/{token}/` public checkout.

### Decisions

- Created [ADR-0006](../adr/0006-event-cost-and-operational-details.md).
- Created [ADR-0007](../adr/0007-graduation-ticket-sales.md).

### Assumptions

- **Confirmed by repository evidence:** `EventBudget` already owns event totals and optional item totals.
- **Confirmed by user instruction:** public graduation checkout is unauthenticated and uses Mercado Pago.
- **Unconfirmed:** whether schedules need structured blocks later; kept as text for now.
- **Unconfirmed:** whether per-graduate ticket caps are needed; multiple purchases are allowed for now.

### Verification evidence

- `.venv/bin/python backend/manage.py test cashflow`
  - **Stage:** before and after implementation
  - **Scope:** backend focused tests
  - **Result:** pass; after implementation 26 tests
- `../.venv/bin/python manage.py test` from `backend/`
  - **Stage:** before and after implementation
  - **Scope:** full backend test suite
  - **Result:** pass; after implementation 26 tests
- `.venv/bin/python backend/manage.py makemigrations --check --dry-run`
  - **Stage:** before and after implementation
  - **Scope:** migration drift
  - **Result:** pass; generated migration `0006` is current
- `npm run build` from `frontend/`
  - **Stage:** before and after frontend changes
  - **Scope:** production frontend build
  - **Result:** pass

### Remaining risks or TODOs

- Validate budget and ticket Mercado Pago checkout/webhooks/refunds against a real sandbox account before production use.
- Add structured schedule blocks only if operations need per-block sorting/filtering.
- Replace the plain PDF generator only if branded/laid-out receipts become a requirement.

## 2026-07-07 — Payments, graduation close, and audit

### User goal

Add cash-close movement export, direct/provider/employee/service/graduation payments, monthly graduation ticket prices, per-graduate caps, final graduation close/export, employee email, service type catalog, and central audit.

### Starting state

- **Branch:** `eventos`
- **Base state:** clean working tree on `eventos`; previous event/graduation work already present.
- **Baseline checks before editing:** focused backend tests pass, full backend tests pass, migration drift check pass, frontend build pass.

### Scope inspected

- `AGENTS.md`, `docs/PROJECT_STATE.md`, ADR index, ADR-0006, ADR-0007, latest `docs/worklog/2026-07.md`
- Backend models, serializers, services, views, URLs, admin, tests, seed command
- Frontend screens for closes, providers, employees, graduation, reports/navigation

### Plan

1. Add backend models/services/API/tests/migration -> verify with focused backend tests and migration check.
2. Add frontend payments/audit/exports/UI labels -> verify with Vite build.
3. Update README, project state, worklog, ADRs -> verify with canonical checks and final diff.

### Implementation

- Added `Employee.email`, `ServiceType`, `CashMovement.service_type`, and seeded common service types.
- Added `GraduationTicketPrice`, `GraduationEvent.max_tickets_per_graduate`, close fields, manual ticket payments, close/export services, and price resolution by date.
- Added `AuditLogEntry`, standard viewset audit hooks, and explicit service audit logs for payments/closes.
- Added daily account close CSV export listing every movement for that account/date, including transfers.
- Added frontend `Pagos` tabs, `Auditoria`, employee alias-bank/email labels, direct provider/employee pay actions, service type quick create, graduation close/export/monthly price UI, and close movement export.

### Decisions

- Created [ADR-0008](../adr/0008-graduation-ticket-pricing-limits-and-close.md); supersedes ADR-0007.
- Created [ADR-0009](../adr/0009-central-audit-log.md).

### Assumptions

- **Confirmed by user instruction:** staff loads monthly ticket prices; system resolves the effective price by date.
- **Confirmed by repository evidence:** close totals already stored transfer totals; export needed movement-level detail.
- **Unconfirmed:** whether pending abandoned public ticket checkouts should expire and release the per-graduate cap.

### Verification evidence

- `.venv/bin/python backend/manage.py test cashflow`
  - **Stage:** before and after implementation
  - **Scope:** backend focused tests
  - **Result:** pass; after implementation 29 tests
- `.venv/bin/python backend/manage.py makemigrations --check --dry-run`
  - **Stage:** after migration
  - **Scope:** migration drift
  - **Result:** pass
- `npm run build` from `frontend/`
  - **Stage:** after frontend changes
  - **Scope:** production frontend build
  - **Result:** pass

### Remaining risks or TODOs

- Run real Mercado Pago sandbox validation for graduation ticket checkout/refund before production use.
- TODO: automatically email employee payment receipt PDFs to `Employee.email` after a payment is registered.
- Audit log records summaries only; add full before/after diffs only if required.

## 2026-07-07 — Event 360, client payment links, and tax history

### User goal

Simplify event operations around one Evento 360 view: create client with event, make base amount plus optionals the final event total, show paid/unpaid details, allow seña/cobro from the event, generate client Mercado Pago links, close events with frozen final numbers, show audit history, and show tax payment history.

### Starting state

- **Branch:** `eventos`
- **Base state:** clean working tree on `eventos` at start of task.
- **Baseline checks before editing:** focused backend tests pass, full backend tests pass, migration drift check pass, frontend build pass.

### Scope inspected

- `AGENTS.md`, `docs/PROJECT_STATE.md`, ADR index, latest `docs/worklog/2026-07.md`
- Backend event, budget, payment, Mercado Pago, cash movement, tax, audit models/services/views/URLs/tests
- Frontend event, graduation public payment, tax, and audit screens

### Plan

1. Add backend Event 360 primitives -> verify with migration check and focused backend tests.
2. Add frontend event/public-payment/tax-history flows -> verify with production build.
3. Record durable decision and project memory -> verify with final canonical checks/status.

### Implementation

- Added event public payment tokens, close metadata, and final amount snapshots.
- Added event statuses `SIGNALED` and `CLOSED`; `DRAFT` now represents presupuesto.
- Added inline client+event creation API and event base amount budget item `Evento`.
- Added manual event seña/cobro service/action that creates approved budget payments and confirmed income cash movements.
- Added public event payment context and preference endpoints for Mercado Pago.
- Added event overview payments and audit entries for Evento 360.
- Added frontend Evento 360 with inline client creation, public payment link, manual cobro/seña, close action, receipt PDF buttons, event audit, and tax payment history/export.

### Decisions

- Created [ADR-0010](../adr/0010-event-360-and-client-payment-links.md).

### Assumptions

- **Confirmed by user instruction:** event total is base amount plus optionals; public event payments use Mercado Pago.
- **Confirmed by repository evidence:** existing budget/payment/cash movement models can carry the flow without a new sales ledger.
- **Unconfirmed:** exact legal/fiscal receipt requirements; receipts remain internal printable PDFs.
- **Unconfirmed:** whether event close should hard-lock every linked edit.

### Verification evidence

- `.venv/bin/python backend/manage.py makemigrations --check --dry-run`
  - **Stage:** after manual migration
  - **Scope:** migration drift
  - **Result:** pass; no changes detected
- `.venv/bin/python backend/manage.py test cashflow`
  - **Stage:** after backend and frontend implementation
  - **Scope:** backend focused tests
  - **Result:** pass; 33 tests
- `../.venv/bin/python manage.py test` from `backend/`
  - **Stage:** after focused tests
  - **Scope:** full backend test suite
  - **Result:** pass; 33 tests
- `npm run build` from `frontend/`
  - **Stage:** after frontend changes
  - **Scope:** production frontend build
  - **Result:** pass
- `git diff --check`
  - **Stage:** final documentation/code check
  - **Scope:** full working-tree diff whitespace/errors
  - **Result:** pass

### Remaining risks or TODOs

- Validate public event checkout/webhook/refund against a real Mercado Pago sandbox before production use.
- Define legal/fiscal receipt requirements before replacing internal printable PDFs.
- Decide whether event close should hard-lock edits across budget items, provider ledger, staff assignments, and cash movements.

## 2026-07-07 — Material UI backoffice and ticket price ranges

### User goal

Make the backoffice feel production-ready and more professional with Material UI, popup notifications, transition/menu navigation, no "Eventos basicos" label, payments living in each feature area, employee payment amount autocomplete, and graduation ticket prices with `vigencia desde`/`vigencia hasta`.

### Starting state

- **Branch:** `neweventos`
- **Pre-existing working-tree changes:** Event 360/payment-link work from the prior task was still uncommitted.

### Scope inspected

- Frontend package manifest, lockfile, app shell, shared form/table helpers, event/graduation/people/payment screens.
- Backend graduation ticket price models/services/views/admin/tests.
- `docs/PROJECT_STATE.md`, ADR index, current worklog.

### Plan

1. Install Material UI -> verify with `npm run build`.
2. Add ticket price `valid_until` -> verify with focused backend tests and migration drift check.
3. Move UX behavior into shared controls/shell -> verify with frontend build.
4. Update ADR/project memory/worklog -> verify with diff/status.

### Implementation

- Added Material UI dependencies and migrated shared text inputs, textareas, selects, tables, page headers, navigation drawer, app bar, theme, screen fade, and Snackbar notifications.
- Removed `Pagos` from the main menu and renamed `Eventos basicos` to `Eventos`.
- Autocompletes employee payment `amount` from selected assignment pending amount.
- Added `GraduationTicketPrice.valid_until`, backend range resolution, admin/API support, and tests.
- Unified graduation link creation so initial ticket price includes `vigencia desde` and `vigencia hasta`.

### Decisions

- Created [ADR-0011](../adr/0011-graduation-ticket-price-validity-ranges.md).
- Created [ADR-0012](../adr/0012-material-ui-backoffice-shell.md).

### Assumptions

- **Confirmed by user instruction:** use Material UI and popup notifications.
- **Confirmed by repository evidence:** shared form/table helpers can migrate most screens without rewriting each feature.
- **Unconfirmed:** whether overlapping graduation price ranges must be blocked; current behavior picks the latest matching range.

### Verification evidence

- `npm install @mui/material @emotion/react @emotion/styled @mui/icons-material`
  - **Stage:** dependency install
  - **Scope:** frontend dependencies
  - **Result:** pass; installed Material UI packages
- `npm audit --omit=dev`
  - **Stage:** after install
  - **Scope:** production dependency audit request
  - **Result:** reports Vite/esbuild dev-server advisories; automatic fix requires breaking Vite major upgrade
- `npm run build` from `frontend/`
  - **Stage:** after UI changes
  - **Scope:** production frontend build
  - **Result:** pass; Vite warns main chunk is above 500 KB
- `.venv/bin/python backend/manage.py test cashflow`
  - **Stage:** after backend changes
  - **Scope:** backend focused tests
  - **Result:** pass; 33 tests
- `.venv/bin/python backend/manage.py makemigrations --check --dry-run`
  - **Stage:** after migration
  - **Scope:** migration drift
  - **Result:** pass

### Remaining risks or TODOs

- Remove Bootstrap CDN after replacing remaining grid utility classes with MUI layout.
- Plan Vite major upgrade separately to resolve dev-server audit advisories.
- Add route-level code splitting if the MUI bundle size becomes measurable in production.

## 2026-07-07 — Backoffice list/detail UX pass

### User goal

Replace the visible multi-column form-heavy backoffice with a more serious list/detail/action pattern using buttons, dialogs/windows, and clearer navigation.

### Starting state

- **Branch:** `neweventos`
- **Pre-existing working-tree changes:** Event 360 and Material UI shell work remained uncommitted.

### Scope inspected

- `frontend/src/App.jsx` screens with open forms: Eventos, Egresados, Personal.
- Shared Material UI helpers and styles.

### Plan

1. Add shared action dialog/action card primitives -> verify with frontend build.
2. Move high-noise forms in Eventos, Egresados, and Personal into dialogs -> verify with frontend build.
3. Keep lists/fichas visible and use buttons for create/edit/pay actions -> verify by static build and diff check.

### Implementation

- Added `ActionDialog` and `ActionCard` shared UX primitives.
- Eventos now opens new event, edit ficha, register cobro, budget header, add item, and edit budget item in dialogs.
- Budget items now render as a table with action buttons instead of inline edit forms.
- Egresados now opens link creation, monthly price, and graduate creation in dialogs.
- Personal now opens employee, role, assignment, payment, and employee ficha forms in dialogs.

### Decisions

- No ADR created. This implements ADR-0012's Material UI backoffice direction without a new architectural decision.

### Assumptions

- **Confirmed by user instruction:** list/detail/action UX is preferred over always-visible forms.
- **Unconfirmed:** exact final information architecture for every secondary screen; this pass focuses on the highest-noise operational screens.

### Verification evidence

- `npm run build` from `frontend/`
  - **Stage:** after UX refactor
  - **Scope:** production frontend build
  - **Result:** pass; Vite warns main chunk is above 500 KB

### Remaining risks or TODOs

- Apply the same dialog/list polish to lower-traffic screens such as Caja, Cuentas, Proveedores, Impuestos, and Reportes.
- Replace remaining Bootstrap grid utilities with MUI layout when doing the next UI cleanup pass.

## 2026-07-07 — Complete dialog UX pass and Vite chunk split

### User goal

Finish the remaining form-heavy secondary screens and solve the Material UI bundle warning with code splitting.

### Starting state

- **Branch:** `neweventos`
- **Pre-existing working-tree changes:** Event 360, Material UI shell, and prior list/detail UX pass remained uncommitted.

### Scope inspected

- `frontend/src/App.jsx` screens: Caja, Cuentas, Transferencias, Proveedores, Impuestos, Reportes.
- `frontend/vite.config.js`.
- Project state and latest worklog.

### Plan

1. Move create/edit/pay forms on secondary screens into dialogs -> verify with frontend build.
2. Add Vite manual chunks for React/MUI/vendor -> verify build output has split chunks and no 500 KB warning.
3. Update project memory -> verify `git diff --check`.

### Implementation

- Caja: new movement and movement detail/edit now open in dialogs.
- Cuentas: new account and balance adjustment now open in dialogs.
- Transferencias: new transfer now opens in a dialog.
- Proveedores: new provider, edit provider, debt/remito, and provider payment now open in dialogs.
- Impuestos: tax payment now opens in a dialog.
- Vite now splits `react`, `mui`, and `vendor` chunks with `manualChunks`.

### Decisions

- No new ADR. This completes ADR-0012's Material UI backoffice direction.

### Assumptions

- **Confirmed by user instruction:** complete the list/detail/dialog UX and code splitting now.
- **Confirmed by build evidence:** manual chunks remove the large main bundle warning.

### Verification evidence

- `npm run build` from `frontend/`
  - **Stage:** after UX and Vite config changes
  - **Scope:** production frontend build
  - **Result:** pass; chunks emitted as `index` 122.48 KB, `react` 142.91 KB, `mui` 260.56 KB, `vendor` 9.05 KB; no large chunk warning
- `git diff --check`
  - **Stage:** after code/docs changes
  - **Scope:** full working-tree diff whitespace/errors
  - **Result:** pass

### Remaining risks or TODOs

- Bootstrap CDN still exists for remaining grid utility classes.
- Vite/esbuild dev-server advisory still requires a separate Vite major upgrade.

## 2026-07-07 — Local Mercado Pago configuration

### User goal

Configure the provided Mercado Pago application/test credentials locally, matching the existing payment flow.

### Starting state

- **Branch:** `neweventos`
- **Pre-existing working-tree changes:** Event 360, Material UI, and list/detail UX work remained uncommitted.

### Scope inspected

- `backend/config/settings.py`, `backend/.env.example`, `.gitignore`, Mercado Pago service/view usages, project state, ADR index, and latest worklog entries.

### Plan

1. Add local env loading with stdlib only -> verify with Django settings checks.
2. Store only app configuration in ignored `backend/.env` -> verify with `git check-ignore`.
3. Record memory without secrets -> verify final diff excludes credential values.

### Implementation

- Added a minimal `backend/.env` loader in Django settings that does not override real process environment variables.
- Created ignored `backend/.env` with Mercado Pago app access token, collector ID, local backend/frontend URLs, and local webhook signature setting.
- Left test usernames, passwords, verification codes, and card numbers out of project files.

### Decisions

- No ADR created. This is local configuration plumbing, not a durable architecture decision.

### Assumptions

- **Confirmed by repository evidence:** the app uses Checkout Pro init points, so no frontend public key is required by current code.
- **Confirmed by user instruction:** use the provided Mercado Pago credentials for local configuration.

### Verification evidence

- `git check-ignore -v backend/.env`
  - **Stage:** after creating local env
  - **Scope:** secret handling
  - **Result:** pass; `backend/.env` is ignored by `.gitignore`
- `.venv/bin/python backend/manage.py check`
  - **Stage:** after settings change
  - **Scope:** Django configuration
  - **Result:** pass; system check identified no issues
- `.venv/bin/python backend/manage.py shell -c "<sanitized settings probe>"`
  - **Stage:** after settings change
  - **Scope:** Mercado Pago settings loading
  - **Result:** pass; token and collector are set, account is `MERCADO PAGO`, local webhook signature enforcement is false

### Remaining risks or TODOs

- Real Mercado Pago checkout/webhook/refund validation is still pending; this task only configured local credentials.
- Production still needs environment variables configured in the host, especially a webhook secret and public HTTPS backend/frontend URLs.

## 2026-07-07 — One-column backoffice layout pass

### User goal

Remove the remaining two-column backoffice composition, move the Event 360 primary action to the header, make filters/totals/detail sections full-width, and keep event additional items payable at any time from the event payment dialog.

### Starting state

- **Branch:** `neweventos`
- **Pre-existing working-tree changes:** Event 360, Material UI shell, Mercado Pago local configuration, and previous UX work remained uncommitted.

### Scope inspected

- `frontend/src/App.jsx` Event 360 header, filters, payment dialog, and public payment pages.
- `frontend/src/styles.css` top-level layout rules.
- `backend/cashflow/serializers.py` to confirm `EventBudgetPaymentSerializer` exposes `budget_item`.

### Plan

1. Move Event 360's "Nuevo evento" action into the page header -> verify the removed helper card text is gone.
2. Force top-level operational rows to one-column full width and make public payment shells single-column -> verify frontend build.
3. Fill manual event payment amount from the selected budget item pending amount -> verify serializer contract and build.

### Implementation

- `PageHeader` now accepts header actions; Event 360 uses it for "Nuevo evento".
- Removed the redundant Event 360 card that said "Lista, filtros y acciones. Los formularios se abren en ventanas."
- Top-level screen columns now span 100%, so filters, lists, totals, and detail blocks stack full-width.
- Public graduation and event-payment pages use `public-shell` instead of the admin grid shell.
- Selecting an event budget item in "Registrar cobro" fills the pending amount and labels items by pending balance.

### Decisions

- No new ADR. This is an implementation detail within ADR-0012's Material UI backoffice direction.

### Assumptions

- **Confirmed by user instruction:** the desired system-wide composition is one top-level column with actions in dialogs/windows.
- **Confirmed by repository evidence:** event budget payments expose `budget_item`, allowing pending-by-item calculation in the UI.

### Verification evidence

- `npm run build` from `frontend/`
  - **Stage:** after one-column layout and Event 360 payment changes
  - **Scope:** production frontend build
  - **Result:** pass; chunks emitted as `index` 122.99 KB, `react` 142.91 KB, `mui` 260.56 KB, `vendor` 9.05 KB
- `git diff --check`
  - **Stage:** after code changes
  - **Scope:** full working-tree diff whitespace/errors
  - **Result:** pass
- Browser verification attempt
  - **Stage:** after build
  - **Scope:** local `127.0.0.1:5173`
  - **Result:** inconclusive; in-app browser connection timed out, while local frontend/backend listeners were present.

### Remaining risks or TODOs

- Visual browser verification should be repeated in the running app because the in-app browser timed out during this pass.
- Bootstrap CDN/grid utilities still exist for inner form composition; top-level screen layout is no longer two-column.

## 2026-07-07 — UX/UI action placement and remaining two-column audit

### User goal

Review whether buttons are placed correctly, remove remaining two-column page sections, and leave the backoffice with a more professional production-ready style.

### Starting state

- **Branch:** `main`
- **Pre-existing working-tree changes:** none reported by `git status` before this pass.

### Scope inspected

- `frontend/src/App.jsx` operational screens: Caja, Cuentas, Transferencias, Proveedores, Personal, Egresados, Impuestos, Evento 360, Dashboard.
- `frontend/src/styles.css` shared shell, card, and grid rules.
- Project state, ADR index, latest worklog, and git status.

### Plan

1. Move primary module actions to page headers -> verify frontend build.
2. Expand remaining operational two-column panel layouts to one column via shared CSS -> verify frontend build and diff check.
3. Record UX decision and verification -> verify working-tree status.

### UX findings applied

- Primary actions belong at the page header level: `Nuevo movimiento`, `Nueva cuenta`, `Nueva transferencia`, `Nuevo proveedor`, `Nuevo empleado`, `Nuevo link`, and `Registrar pago`.
- Contextual actions remain beside selected records because they depend on local context: provider debt/payment, employee assignment/payment, event budget item actions.
- Remaining two-column operational panels were caused by nested Bootstrap `col-lg-*`, `col-xl-*`, and selected `col-md-*` classes under `.main-stage`.

### Implementation

- Added header actions to Caja, Cuentas, Transferencias, Proveedores, Personal, Egresados, and Impuestos.
- Removed action-only cards that duplicated the page title and consumed a left column.
- Broadened shared one-column CSS rules for remaining operational panel columns while preserving form fields and metric cards.
- Made page-header and action-card buttons compact and right-aligned so they do not consume vertical volume or sit next to the title text.

### Decisions

- No new ADR. This continues ADR-0012's Material UI backoffice direction.

### Assumptions

- **Confirmed by user instruction:** the backoffice should avoid two-column page composition and prefer professional list/detail/action placement.
- **Agent assumption:** metric cards can remain horizontal because they are scanning summaries, not page-level split layouts.

### Verification evidence

- `npm run build` from `frontend/`
  - **Stage:** after action-placement, compact header buttons, and layout changes
  - **Scope:** production frontend build
  - **Result:** pass; chunks emitted as `index` 122.37 KB, `react` 142.91 KB, `mui` 260.56 KB, `vendor` 9.05 KB
- `git diff --check`
  - **Stage:** after code changes
  - **Scope:** full working-tree diff whitespace/errors
  - **Result:** pass

### Remaining risks or TODOs

- Visual browser verification still needs a live browser pass; this pass used deterministic build and CSS/code inspection.
- Bootstrap grid utilities remain for inner forms; replacing every inner form grid with MUI Grid is larger than needed for the reported layout issue.

## 2026-07-08 — Responsive shell and collapsible sidebar

### User goal

Make the system fully responsive, with the sidebar expanded on desktop, collapsed on mobile, and manually collapsible.

### Starting state

- **Branch:** `main`
- **Base commit:** `fa299cd`
- **Pre-existing working-tree changes:** backend payment/service/test files, previous frontend UX/layout edits, and docs were already modified.

### Scope inspected

- `frontend/src/App.jsx` Material UI drawer, app bar, page header, action dialogs, and navigation behavior.
- `frontend/src/styles.css` responsive layout, card, table, and mobile rules.
- `docs/PROJECT_STATE.md`, ADR index, latest worklog, and git status.

### Plan

1. Add responsive drawer state using existing Material UI primitives -> verify build.
2. Add mobile-friendly shell/dialog/overflow CSS -> verify build and diff check.
3. Try browser breakpoint verification against the running Vite app -> record result.

### Implementation

- Drawer now uses Material UI responsive breakpoints: persistent and open by default on desktop, temporary and closed by default on mobile.
- Added top-bar menu toggle, in-drawer collapse button, and mobile auto-close after navigation.
- Action dialogs become full-screen on small screens.
- Added overflow guards for main content, cards, metric values, tables, and compact mobile spacing.

### Decisions

- No new ADR. This is an implementation detail under ADR-0012's Material UI backoffice shell decision.

### Assumptions

- **Confirmed by user instruction:** desktop should show the sidebar expanded and mobile should start collapsed.
- **Repository evidence:** Material UI is already installed and used for the shell, so no new dependency was needed.

### Verification evidence

- `npm run build` from `frontend/`
  - **Stage:** after responsive drawer/dialog/CSS changes
  - **Scope:** production frontend build
  - **Result:** pass; chunks emitted as `index` 123.21 KB, `react` 142.91 KB, `mui` 262.23 KB, `vendor` 9.05 KB
- `git diff --check`
  - **Stage:** after code changes
  - **Scope:** full working-tree diff whitespace/errors
  - **Result:** pass
- Browser verification against `http://127.0.0.1:5173/`
  - **Stage:** after build, using the already-running Vite server
  - **Scope:** local app shell
  - **Result:** blocked at login screen; authenticated sidebar shell was not visible in the browser session.

### Remaining risks or TODOs

- Repeat browser verification after logging into the local app: desktop 1280px sidebar open, mobile 390px sidebar closed, menu opens/closes, and no horizontal overflow.
- Existing unrelated backend and docs changes remain in the working tree and were not modified for this task.

## 2026-07-08 — Receipt PDF email delivery

### User goal

After every registered payment or money intake, generate a downloadable receipt and email it for both manual and Mercado Pago payments.

### Starting state

- **Branch:** `main`
- **Pre-existing working-tree changes:** responsive shell/frontend UX and docs were already modified.

### Scope inspected

- `backend/cashflow/services.py` cash movement receipt PDF, event payments, ticket purchases, provider/employee/service/tax payments.
- `backend/cashflow/views.py` public/manual payment endpoints and cash movement receipt endpoint.
- `backend/cashflow/models.py`, migrations, serializers, and focused payment tests.
- `frontend/src/App.jsx` event manual payment dialog and public event payment page.

### Plan

1. Reuse existing `CashMovement` PDF receipt -> verify existing receipt tests still pass.
2. Add post-commit email sender and wire manual/MP payment flows -> verify mail outbox tests.
3. Store public event receipt email through Mercado Pago webhook delay -> verify migration drift and public preference test.

### Implementation

- Added `EventBudgetPayment.receipt_email` and migration `0010_eventbudgetpayment_receipt_email`.
- Added `send_cash_movement_receipt_email()` using existing receipt PDF, `EmailMessage`, and `transaction.on_commit`.
- Wired receipts for event manual payments, event Mercado Pago approvals, graduation manual/MP payments, provider payments, employee payments, service payments, tax payments, and direct confirmed cash movements.
- Public event payment page now requires `Email para comprobante`; manual event cobro dialog has `Email comprobante` and preloads the event/client email when available.
- Removed obsolete employee receipt TODO comments.

### Decisions

- Created [ADR-0013](../adr/0013-cash-movement-receipt-emails.md).

### Assumptions

- **Confirmed by user instruction:** manual and Mercado Pago receipts should be emailed.
- **Confirmed by repository evidence:** confirmed `CashMovement` is the existing receipt source and download endpoint.
- **Confirmed existing constraint:** receipts remain internal printable PDFs until legal/fiscal requirements are defined.

### Verification evidence

- `../.venv/bin/python manage.py makemigrations --check --dry-run` from `backend/`
  - **Stage:** after migration
  - **Scope:** migration drift
  - **Result:** pass; no changes detected
- `../.venv/bin/python manage.py test cashflow.tests.Event360Tests cashflow.tests.EventBudgetPaymentStageThreeTests cashflow.tests.GraduationTicketTests cashflow.tests.PaymentsAndAuditTests` from `backend/`
  - **Stage:** after receipt email implementation
  - **Scope:** focused event/payment/graduation/provider/employee/service tests
  - **Result:** pass; 13 tests
- `../.venv/bin/python manage.py test cashflow` from `backend/`
  - **Stage:** after focused tests
  - **Scope:** full cashflow suite
  - **Result:** pass; 33 tests
- `npm run build` from `frontend/`
  - **Stage:** after public/manual event email UI changes
  - **Scope:** production frontend build
  - **Result:** pass; chunks emitted as `index` 123.69 KB, `react` 142.91 KB, `mui` 262.23 KB, `vendor` 9.05 KB
- `git diff --check`
  - **Stage:** after code/docs changes
  - **Scope:** full working-tree diff whitespace/errors
  - **Result:** pass

### Remaining risks or TODOs

- Production SMTP settings must be configured and validated; local DEBUG uses console email backend.
- Receipt delivery failures are logged and do not rollback the payment/cash movement.
- Legal/fiscal receipt requirements are still undefined; current PDFs remain internal receipts.

## 2026-07-08 — Parametrized event types and Egresados selection

### User goal

Parametrize event types as cumpleaños de 15, egresados, evento privado, and casamiento. Events of type Egresados should be selectable from the Egresados menu.

### Starting state

- **Branch:** `main`
- **Base commit:** `fa299cd`
- **Pre-existing working-tree changes:** responsive shell, receipt email flow, employee alias/name display correction, docs, and receipt-email migration/ADR were already modified or untracked.

### Scope inspected

- `backend/cashflow/models.py`, `serializers.py`, `services.py`, migrations, seed data, and event/graduation tests.
- `frontend/src/App.jsx` Evento 360 and Egresados screens.
- Project state, ADR index, latest worklog, and git status.

### Plan

1. Add fixed event-type choices and legacy normalization migration -> verify migration drift and backend tests.
2. Restrict Egresados links to base events of type Egresados -> verify API test.
3. Replace free-text event-type UI with selects and filter Egresados base-event menu -> verify frontend build.

### Implementation

- Added `Event.Type` choices: `QUINCE`, `EGRESADOS`, `EVENTO_PRIVADO`, and `CASAMIENTO`.
- Added migration `0011_parametrize_event_type` to map legacy strings like `Boda`, `Cumple`, `Social`, and unknown values into the fixed choices.
- `GraduationEventSerializer` now rejects base events whose type is not `EGRESADOS`.
- Evento 360 uses selects for type on create, edit, and filter; cards show human labels.
- Egresados "Evento base" only lists events typed as Egresados that do not already have a graduation link.

### Decisions

- Created [ADR-0014](../adr/0014-parametrized-event-types.md).

### Assumptions

- **Confirmed by user instruction:** the canonical type list is exactly cumpleaños de 15, egresados, evento privado, and casamiento.
- **Confirmed by repository evidence:** `GraduationEvent` already has a one-to-one `Event` relation, so filtering base events is sufficient.

### Verification evidence

- `../.venv/bin/python manage.py makemigrations --check --dry-run` from `backend/`
  - **Stage:** after adding manual migration
  - **Scope:** migration drift
  - **Result:** pass; no changes detected
- `../.venv/bin/python manage.py test cashflow.tests.EventModuleStageOneTests cashflow.tests.Event360Tests cashflow.tests.GraduationTicketTests` from `backend/`
  - **Stage:** after backend and frontend-facing contract changes
  - **Scope:** focused event/graduation behavior
  - **Result:** pass; 9 tests
- `../.venv/bin/python manage.py test cashflow` from `backend/`
  - **Stage:** after focused tests
  - **Scope:** full cashflow suite
  - **Result:** pass; 34 tests
- `npm run build` from `frontend/`
  - **Stage:** after event-type select changes
  - **Scope:** production frontend build
  - **Result:** pass; chunks emitted as `index` 124.54 KB, `react` 142.91 KB, `mui` 262.23 KB, `vendor` 9.05 KB
- `git diff --check`
  - **Stage:** after code/docs changes
  - **Scope:** full working-tree diff whitespace/errors
  - **Result:** pass

### Remaining risks or TODOs

- No browser-authenticated visual pass was run for the select menus.
- Existing unrelated receipt-email, responsive shell, employee alias/name, and docs changes remain in the working tree.

## 2026-07-08 — Employee alias is not display name

### User goal

Fix employee fields because the bank alias was appearing as the employee name.

### Scope inspected

- `Employee.__str__()`, `EmployeeSerializer.display_name`, employee selects/tables in `frontend/src/App.jsx`, and payment/audit tests.
- Existing uncommitted receipt and UX changes remained in the working tree.

### Implementation

- Changed `Employee.__str__()` to always return first name plus last name.
- Kept `alias` as a separate employee field and added an `Alias` column to the employee list.
- Added a regression assertion that an employee with alias still displays as `Eva Diaz`.

### Decisions

- No ADR. This is a field-display bug fix, not a durable architecture decision.

### Verification evidence

- `../.venv/bin/python manage.py test cashflow.tests.PaymentsAndAuditTests` from `backend/`
  - **Result:** pass; 1 test
- `../.venv/bin/python manage.py test cashflow.tests.EventModuleStageOneTests.test_can_create_event_with_extended_stage_one_fields cashflow.tests.Event360Tests.test_create_event_with_client_and_amount_creates_base_budget_item` from `backend/`
  - **Result:** pass; 2 tests after checking transient full-suite failures
- `../.venv/bin/python manage.py makemigrations --check --dry-run` from `backend/`
  - **Result:** pass; no changes detected
- `../.venv/bin/python manage.py test cashflow` from `backend/`
  - **Result:** pass; 34 tests
- `npm run build` from `frontend/`
  - **Result:** pass
- `git diff --check`
  - **Result:** pass

### Remaining risks or TODOs

- Existing records that were manually typed with the alias in `first_name` still need manual data cleanup; the code no longer causes that display mix-up.

## 2026-07-08 — MUI dashboard charts

### User goal

Use Material UI components and best practices, and include charts in the dashboard.

### Scope inspected

- `frontend/src/App.jsx` dashboard, shared MUI helpers, and existing dashboard API usage.
- `backend/cashflow/services.py` dashboard summary shape.
- ADR-0012 Material UI backoffice shell, project state, latest worklog, and git status.

### Plan

1. Replace dashboard Bootstrap/card layout with MUI `Grid`, `Card`, `Stack`, `Chip`, and loading state -> verify frontend build.
2. Add lightweight charts from existing `/dashboard/` data -> verify no new dependency or backend contract change.
3. Update project memory -> verify whitespace/diff checks.

### Implementation

- Added MUI dashboard metric/panel helpers.
- Added a daily income/expense SVG chart and account-balance bar chart using existing dashboard fields.
- Kept tables and API contract unchanged; no dependency or backend endpoint added.

### Decisions

- No ADR. This implements the already accepted Material UI shell decision in ADR-0012.

### Assumptions

- **Confirmed by user instruction:** dashboard should include charts.
- **Confirmed by repository evidence:** `/dashboard/` already exposes enough data for the requested charts.

### Verification evidence

- `npm run build` from `frontend/`
  - **Stage:** after frontend changes
  - **Scope:** production frontend build
  - **Result:** pass; chunks emitted as `index` 125.58 KB, `react` 142.91 KB, `mui` 272.80 KB, `vendor` 9.05 KB
- Browser check at `http://localhost:5173/`
  - **Stage:** after user confirmed dev server was already running
  - **Scope:** local frontend load and console
  - **Result:** partial pass; app loaded the login screen with title `Caja Moments` and no console errors. Dashboard visual state was not reached because the browser session was unauthenticated.

### Remaining risks or TODOs

- No authenticated dashboard visual pass was run; `npm run dev` failed inside the sandbox with `listen EPERM 0.0.0.0:5173`, the escalated retry was rejected, then the already-running server was verified only up to login.
- Existing unrelated backend/docs/frontend changes remain in the working tree.

## 2026-07-08 — Public graduation payment dropdown lists graduates

### User goal

In the public payment link, show the graduates list in the dropdown.

### Scope inspected

- `PublicGraduationPage` in `frontend/src/App.jsx`.
- `GraduationEventGraduateSearchAPIView` in `backend/cashflow/views.py`.
- `GraduationTicketTests` public checkout coverage.

### Plan

1. Use the existing public graduates endpoint without a search query -> verify focused graduation tests.
2. Keep the select's built-in filtering for long lists -> verify frontend build.
3. Record the small public-link behavior change -> verify diff check.

### Implementation

- The public graduates endpoint now returns the event's graduates ordered by last name and first name, even without a search query.
- The public graduation payment page loads that list on page load.
- Removed the external "Buscar egresado" field; the dropdown already provides an internal search when there are many options.

### Decisions

- No new ADR. This is a UI/API behavior fix within the existing graduation payment flow.

### Verification evidence

- `../.venv/bin/python manage.py test cashflow.tests.GraduationTicketTests` from `backend/`
  - **Stage:** after endpoint/UI change
  - **Scope:** public graduation list/search/checkout behavior
  - **Result:** pass; 2 tests
- `npm run build` from `frontend/`
  - **Stage:** after public dropdown change
  - **Scope:** production frontend build
  - **Result:** pass; chunks emitted as `index` 124.38 KB, `react` 142.91 KB, `mui` 262.23 KB, `vendor` 9.05 KB
- `git diff --check`
  - **Stage:** after code/docs changes
  - **Scope:** full working-tree diff whitespace/errors
  - **Result:** pass

### Remaining risks or TODOs

- Very large graduate lists may eventually need server-side pagination/autocomplete; current implementation intentionally keeps the simple dropdown requested.

## 2026-07-08 — System audit and professional README refresh

### User goal

Audit the whole system, identify missing/failing areas, ask questions if needed, and make the README professional.

### Starting state

- **Branch:** `main`
- **Base commit:** `15920cc responsive`
- **Pre-existing working-tree changes:** none from `git status --short --branch`

### Scope inspected

- Root instructions, project state, ADR index, latest July worklog, git status.
- README, backend settings/routes/models/serializers/services/views/tests, env examples, deploy scripts, Render/Vercel config.
- Frontend package metadata, Vite config surface, `App.jsx`, `styles.css`, and Bootstrap CDN use.

### Plan

1. Run current backend/frontend/deploy/dependency checks -> record pass/fail evidence.
2. Rewrite README around current scope, setup, API, deploy, audit gaps, and known failures.
3. Update project memory/worklog -> verify Markdown diff and final status.

### Implementation

- Rewrote `README.md` as an operational project document with audited status, stack, architecture, modules, rules, API, setup, verification, env vars, seeds, and deploy notes.
- Added explicit "Que falta" and "Donde falla o tiene deuda" sections based on repository evidence and checks.
- Updated `docs/PROJECT_STATE.md` with the current commit, active work, dependency-audit failure, missing CI/frontend tests/rate limiting, and local LibreSSL warning.

### Decisions

- No ADR. This is documentation and audit reporting of existing behavior, not a new technical decision.

### Verification evidence

- `../.venv/bin/python manage.py test` from `backend/`
  - **Stage:** before README rewrite
  - **Scope:** full backend test suite
  - **Result:** pass; 34 tests
- `../.venv/bin/python manage.py makemigrations --check --dry-run` from `backend/`
  - **Stage:** before README rewrite
  - **Scope:** migration drift
  - **Result:** pass; no changes detected
- `npm run build` from `frontend/`
  - **Stage:** before README rewrite
  - **Scope:** production frontend build
  - **Result:** pass
- `env DEBUG=False SECRET_KEY=prod-like-secret-key-with-enough-length-for-django-checks ALLOWED_HOSTS=caja-moments-api.onrender.com CORS_ALLOWED_ORIGINS=https://caja-moments-web.onrender.com CSRF_TRUSTED_ORIGINS=https://caja-moments-web.onrender.com SECURE_SSL_REDIRECT=True ../.venv/bin/python manage.py check --deploy` from `backend/`
  - **Stage:** before README rewrite
  - **Scope:** production-like Django deploy settings
  - **Result:** pass; no issues
- `npm audit --omit=dev` from `frontend/`
  - **Stage:** before README rewrite
  - **Scope:** frontend dependency advisories
  - **Result:** fail; 2 vulnerabilities in Vite/esbuild dev-server chain; force fix requires Vite 8

### Assumptions

- **Confirmed by repository evidence:** audit gaps come from checks, code structure, docs, package metadata, and deploy config.
- **Unconfirmed:** production Mercado Pago behavior remains unvalidated until sandbox/live tests are run against a real account.

### Remaining risks or TODOs

- Prioritize CI, frontend smoke/E2E test, Vite/esbuild upgrade path, public endpoint rate limiting, Mercado Pago sandbox validation, backup/restore drill, and fiscal receipt requirements.

## 2026-07-08 — Public graduation checkout Mercado Pago header and total detail

### User goal

Fix the public graduation payment error after pressing "Continuar a Mercado Pago" and show subtotal/total detail before paying.

### Starting state

- **Branch:** `main`
- **Base commit:** `15920cc responsive`
- **Pre-existing working-tree changes:** README, project state, and worklog audit documentation updates from the prior task.

### Scope inspected

- User-provided Django debug page showing `InvalidHeader at /api/ticket-purchases/create-preference/`.
- `MercadoPagoClient` preference creation for budget and ticket payments.
- Public graduation payment page in `frontend/src/App.jsx`.
- Graduation and event payment tests.

### Plan

1. Cast Mercado Pago idempotency headers to string -> verify with a focused regression test.
2. Show unit price, quantity, and total in the public graduation form -> verify frontend build.
3. Re-run related event payment tests because the shared header pattern was fixed there too.

### Implementation

- Converted event-budget and ticket-purchase `x-idempotency-key` headers to `str(...)` before calling the Mercado Pago SDK.
- Added a regression test that checks the ticket checkout header is a string.
- Added unit price, quantity, and total-to-pay detail before the public graduation Mercado Pago button; the button is disabled when total is zero.

### Decisions

- No ADR. This is a bug fix and small UI detail within the accepted Mercado Pago/graduation flow.

### Verification evidence

- `../.venv/bin/python manage.py test cashflow.tests.GraduationTicketTests` from `backend/`
  - **Stage:** after backend fix
  - **Scope:** public graduation checkout, payment sync, and header regression
  - **Result:** pass; 3 tests
- `../.venv/bin/python manage.py test cashflow.tests.EventBudgetPaymentStageThreeTests cashflow.tests.Event360Tests.test_public_event_payment_context_and_preference` from `backend/`
  - **Stage:** after shared Mercado Pago header fix
  - **Scope:** event-budget Mercado Pago preference/payment tests
  - **Result:** pass; 7 tests
- `../.venv/bin/python manage.py makemigrations --check --dry-run` from `backend/`
  - **Stage:** after code changes
  - **Scope:** migration drift
  - **Result:** pass; no changes detected
- `npm run build` from `frontend/`
  - **Stage:** after public checkout UI change
  - **Scope:** production frontend build
  - **Result:** pass

### Assumptions

- **Confirmed by repository evidence:** `idempotency_key` fields use `uuid.uuid4` defaults and can reach the SDK as UUID objects before reload.
- **Confirmed by user evidence:** the public ticket checkout surfaced Django's `InvalidHeader` debug HTML.

### Remaining risks or TODOs

- Real Mercado Pago sandbox/live checkout and webhook validation is still pending; this fix was verified with mocked SDK tests and local build.

## 2026-07-08 — Interactive public graduation checkout UX

### User goal

Make the public ticket purchase page better and more interactive for real clients who may not know the system.

### Starting state

- **Branch:** `main`
- **Base commit:** `15920cc responsive`
- **Pre-existing working-tree changes:** README audit docs plus public Mercado Pago idempotency-header fix.

### Scope inspected

- `PublicGraduationPage` in `frontend/src/App.jsx`.
- Existing shared public shell/card/form styles in `frontend/src/styles.css`.
- Latest project state and worklog.

### Plan

1. Replace the simple public graduation form with a guided purchase flow -> verify frontend build.
2. Add focused responsive CSS without new dependencies -> verify production build.
3. Attempt visual browser check if local Vite can run -> record result.

### Implementation

- Reworked the public graduation page into a guided checkout with visible steps, event header, selected graduate confirmation, quantity +/- controls, email capture, sticky summary, subtotal, and total.
- The Mercado Pago button now shows the exact amount to pay and disables until graduate, email, and positive total are present.
- Added responsive purchase-specific CSS; no new dependency or backend contract change.

### Decisions

- No ADR. This is UX work inside the existing public graduation purchase flow.

### Verification evidence

- `npm run build` from `frontend/`
  - **Stage:** after UX changes
  - **Scope:** production frontend build
  - **Result:** pass
- `npm run dev -- --host 127.0.0.1 --port 5201` from `frontend/`
  - **Stage:** after build
  - **Scope:** attempted local visual verification
  - **Result:** failed in sandbox with `listen EPERM 127.0.0.1:5201`; escalated retry was rejected by user

### Assumptions

- **Confirmed by user instruction:** the public ticket purchase page is client-facing and should be clearer for non-technical users.
- **Confirmed by repository evidence:** existing public endpoint already exposes event name, date, ticket price, and max-per-graduate data needed for the UI.

### Remaining risks or TODOs

- Browser visual QA still needs to be run in an allowed local dev-server session.

## 2026-07-08 — Event optional services visible in Evento 360

### User goal

Make event optionals/services extra visible and usable: add them to events, let clients pay by Mercado Pago link, and let admin register manual collection anytime.

### Scope inspected

- Existing `EventBudgetItem.is_optional` backend model/API.
- Event payment preference and item manual payment endpoints.
- Evento 360 budget/payment UI in `frontend/src/App.jsx`.

### Implementation

- Added explicit `Agregar opcional` and `Agregar base` actions in the commercial budget header.
- Opening `Agregar opcional` pre-fills the item as optional with "Opcional" category and "servicio" unit.
- Made the client payment link visible with copy/open actions; the public link already lets the client pick pending base or optional items.
- Renamed per-item payment action to `Link MP`.
- Replaced the old prompt-based manual item collection with the normal event collection dialog prefilled with item, pending amount, non-deposit flag, and receipt email.
- Event payment item selector now labels items as `Base` or `Opcional`.

### Decisions

- No ADR. The data model and payment behavior already existed; this exposes the existing flow in the UI.

### Verification evidence

- `npm run build` from `frontend/`
  - **Stage:** after Evento 360 UI changes
  - **Scope:** production frontend build
  - **Result:** pass

### Remaining risks or TODOs

- Browser visual QA still needs to be run in an allowed local dev-server session.

## 2026-07-08 — MUI autocomplete for public graduate selection

### User goal

Use a Material UI autocomplete for the public graduation buyers list.

### Scope inspected

- `PublicGraduationPage` and existing MUI imports in `frontend/src/App.jsx`.
- Current public checkout UX worklog/project state.

### Implementation

- Replaced the public graduation `SelectInput` with MUI `Autocomplete`.
- Kept the existing backend payload shape by storing the selected graduate id in `form.graduate`.
- Added empty-state copy for names not found.

### Decisions

- No ADR. This is a UI control swap inside the accepted public graduation checkout flow.

### Verification evidence

- `npm run build` from `frontend/`
  - **Stage:** after autocomplete change
  - **Scope:** production frontend build
  - **Result:** pass

### Remaining risks or TODOs

- Browser visual QA still needs to be run in an allowed local dev-server session.
